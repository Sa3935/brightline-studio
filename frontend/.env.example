# Base URL of the backend API (no trailing slash).
# Local dev: http://localhost:5000
# Production: your deployed backend's HTTPS URL, e.g. https://your-backend.vercel.app
VITE_API_URL=http://localhost:5000
